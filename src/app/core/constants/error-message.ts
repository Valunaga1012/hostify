import { ErrorMessage } from "@pp/domain/interfaces/error-mesage.interface";

export const errorMessages: ErrorMessage = {
  required: 'Este camp es requerido',
  pattern: 'Email no es valido',
  minlength: 'Esta campo debe tener minimo 5 caracteres'
}
